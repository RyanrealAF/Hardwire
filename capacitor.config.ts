import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hardwiremethod.app',
  appName: 'The Hardwire Method',
  webDir: 'dist'
};

export default config;
